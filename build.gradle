/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * User Manual available at https://docs.gradle.org/5.6.2/userguide/java_library_plugin.html
 */

buildscript {
    repositories {
        jcenter()
    }
}

plugins {
    id "com.jfrog.bintray" version "1.8.4"
    id 'maven-publish'
    id 'java-library'
}

repositories {
    jcenter()
}

dependencies {

}

sourceCompatibility = '1.7'
targetCompatibility = '1.7'


task sourcesJar(type: Jar) {
    from sourceSets.main.allJava
    archiveClassifier = 'sources'
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = 'org.bladetools'
            artifactId = 'bladelib'
            version = '1.0'
            artifact sourcesJar
            from components.java
        }
    }
}

bintray {
    user = project.hasProperty('bintrayUser') ? project.property('bintrayUser') : System.getenv('BINTRAY_USER')
    key = project.hasProperty('bintrayApiKey') ? project.property('bintrayApiKey') : System.getenv('BINTRAY_API_KEY')
    publications = ['mavenJava']
    pkg {
        repo = 'bladetools'
        name = 'bladelib'
        userOrg = user
        licenses = ['Apache-2.0']
        vcsUrl = 'https://github.com/bladetools/BladeLib.git'
        labels = ['blade', 'bladelib']
        publicDownloadNumbers = true
        attributes= ['a': ['ay1', 'ay2'], 'b': ['bee'], c: 'cee'] //Optional package-level attributes
        version {
            name = '1.0'
            desc = 'Blade library for module development'
        }
    }
}
